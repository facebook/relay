==================================== INPUT ====================================
# expected-to-throw
{
    friends(
        arg: .5
    ) {
        count
    }
}
==================================== ERROR ====================================
Error: Invalid float literal, fractional float literals require a leading 0, e.g. 0.5 instead of .5 at missing_zero_on_float.invalid.graphql:4:14:
        arg: .5
    ) {
