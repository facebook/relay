language: node_js

git:
  depth: 5

node_js:
  - '7'
  - '6'

script: 
  - if [[ "$TRAVIS_JOB_NUMBER" == *.1 ]]; then npm run typecheck && npm run jest -- --runInBand --no-watchman; else npm run jest -- --runInBand --no-watchman; fi

