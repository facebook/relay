==================================== INPUT ====================================
# When a type implements both Node and @fetchable, setting
# preferFetchable: false should use the Node refetch strategy
# (same as omitting the argument). Before the fix, the boolean
# value was ignored and presence alone triggered fetchable-preferred.
fragment RefetchableFragment on FetchableType
  @refetchable(queryName: "RefetchableFragmentQuery", preferFetchable: false) {
  fetch_id
}
==================================== OUTPUT ===================================
query RefetchableFragmentQuery(
  $id: ID!
) @__RefetchableDerivedFromMetadata
# RefetchableDerivedFromMetadata(
#     FragmentDefinitionName(
#         "RefetchableFragment",
#     ),
# )
 {
  fetch__FetchableType(fetch_id: $id) {
    ...RefetchableFragment
  }
}

fragment RefetchableFragment on FetchableType @refetchable(queryName: "RefetchableFragmentQuery", preferFetchable: false) @__RefetchableMetadata
# RefetchableMetadata {
#     operation_name: OperationDefinitionName(
#         "RefetchableFragmentQuery",
#     ),
#     path: [
#         "fetch__FetchableType",
#     ],
#     identifier_info: Some(
#         RefetchableIdentifierInfo {
#             identifier_field: "fetch_id",
#             identifier_query_variable_name: "id",
#         },
#     ),
#     is_prefetchable_pagination: false,
# }
 {
  fetch_id
}
