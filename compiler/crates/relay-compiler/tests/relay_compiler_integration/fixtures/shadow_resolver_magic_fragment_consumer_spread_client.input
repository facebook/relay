//- UserCombinedPage.js
// Consumer authors a CONCRETE client-extension-typed fragment spread on a
// magic-fragment field. `combined_page` returns the client interface `IPage`,
// implemented by the SERVER type `Page` AND the CLIENT model type `ClientPage`.
// The consumer spreads `...ClientFields` where `fragment ClientFields on
// ClientPage`.
//
// The spread's type condition is a client-extension type, so it is
// DROPPED from the server transplant (its data is served by the model-resolver
// edge minted in `client_edges`, never fetched from the server). The reader keeps
// `...ClientFields` as a masked `$fragmentSpreads` ref read off that edge.
/**
 * @relayField User.combined_page: IPage
 * @rootFragment UserCombinedPageResolverFragment
 * @returnFragment UserCombinedPageReturnFragment
 */
graphql`fragment UserCombinedPageResolverFragment on User {
  page {
    ...UserCombinedPageReturnFragment
  }
}`

//- ClientPageResolver.js
/**
 * @relayType ClientPage implements IPage
 */
export function ClientPage() {}

//- ClientPageTitleResolver.js
/**
 * @relayField ClientPage.title: String
 */
export function title() {}

//- ClientFields.js
graphql`fragment ClientFields on ClientPage {
  title
}`

//- UserConsumer.js
graphql`query UserConsumerQuery {
  me {
    combined_page {
      ...ClientFields @alias(as: "clientFields")
    }
  }
}`

//- relay.config.json
{
  "language": "flow",
  "jsModuleFormat": "haste",
  "schema": "schema.graphql",
  "schemaExtensions": ["schema-extensions"],
  "featureFlags": {
    "enable_shadow_resolvers": { "kind": "enabled" },
    "relay_resolver_enable_interface_output_type": { "kind": "enabled" }
  }
}

//- schema.graphql
type Query { me: User, node(id: ID!): Node }
interface Node { id: ID! }
type User { name: String, page: Page }
type Page implements Node { id: ID!, title: String }

//- schema-extensions/extension.graphql
interface IPage implements Node {
  id: ID!
  title: String
}

extend type Page implements IPage
