# expected-to-throw
#
# Test: DANGEROUSLY_THROW_ON_SEMANTICALLY_NULLABLE_FIELD cannot be used on schema non-null fields
# Property: Schema non-null fields (e.g., id: ID!) should not allow DANGEROUSLY_THROW_ON_SEMANTICALLY_NULLABLE_FIELD action
# Expected: Error suggesting to use THROW instead

query MyQuery {
  me {
    id @required(action: DANGEROUSLY_THROW_ON_SEMANTICALLY_NULLABLE_FIELD)
  }
}
