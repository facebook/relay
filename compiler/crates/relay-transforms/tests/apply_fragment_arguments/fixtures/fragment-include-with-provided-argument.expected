==================================== INPUT ====================================
query TestQuery($id: ID!) {
  node(id: $id) {
    id
    ...TestFragment
  }
}

fragment TestFragment on User
  @argumentDefinitions(foo_gk: {type: "Boolean!", provider: "fooGKProvider"})
{
    name @include(if: $foo_gk)
}
==================================== OUTPUT ===================================
query TestQuery(
  $id: ID!
  $__pv__fooGKProvider: Boolean! @__ProvidedVariableMetadata
  # ProvidedVariableMetadata {
  #     module_name: "fooGKProvider",
  # }
  
) {
  node(id: $id) {
    id
    ...TestFragment
  }
}

fragment TestFragment on User {
  name @include(if: $__pv__fooGKProvider)
}
