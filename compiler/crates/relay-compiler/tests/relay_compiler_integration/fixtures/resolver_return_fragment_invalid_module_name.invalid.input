//- User_foo.js
// Test that @returnFragment requires fragment names to start with the module name.
/**
 * @relayField User.foo: RelayResolverValue
 * @returnFragment WrongPrefix_fragment
 */

//- UserFragment.js
graphql`fragment UserFragment on User {
  foo
}`

//- relay.config.json
{
  "language": "flow",
  "jsModuleFormat": "haste",
  "schema": "schema.graphql",
  "featureFlags": {
    "enable_shadow_resolvers": { "kind": "enabled" }
  }
}

//- schema.graphql
type Query { me: User }
type User { name: String }
