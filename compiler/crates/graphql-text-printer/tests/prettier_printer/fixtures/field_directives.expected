==================================== INPUT ====================================
query UserQuery {
  user {
    email @skip(if: true) @deprecated(reason: "Use primaryEmail instead")
    name @include(if: $showName)
  }
}
==================================== OUTPUT ===================================
query UserQuery {
  user {
    email @skip(if: true) @deprecated(reason: "Use primaryEmail instead")
    name @include(if: $showName)
  }
}
