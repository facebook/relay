[toolchain]
# Should stay in sync with fbcode/buck2/rust-toolchain.toml
# And references in .github/workflows/ci.yml and docusaurus.yml
channel = "nightly-2026-07-05"
targets = ["x86_64-unknown-linux-musl"]
