//- User_foo.js
// Test that @returnFragment rejects invalid GraphQL identifiers.
/**
 * @relayField User.foo: RelayResolverValue
 * @returnFragment 123InvalidName
 */

//- UserFragment.js
graphql`fragment UserFragment on User {
  foo
}`

//- relay.config.json
{
  "language": "flow",
  "jsModuleFormat": "haste",
  "schema": "schema.graphql",
  "featureFlags": {
    "enable_shadow_resolvers": { "kind": "enabled" }
  }
}

//- schema.graphql
type Query { me: User }
type User { name: String }
