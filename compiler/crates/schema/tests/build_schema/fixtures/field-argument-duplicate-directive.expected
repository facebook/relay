==================================== INPUT ====================================
# expected-to-throw

directive @bar on ARGUMENT_DEFINITION

type Foo {
  field(a: String @bar @bar): ID
}

type Query {
  foo: Foo
}
==================================== ERROR ====================================
✖︎ The directive `@bar` can only be used once at this location, but was used 2 times.

  field-argument-duplicate-directive.graphql:6:9
    5 │ type Foo {
    6 │   field(a: String @bar @bar): ID
      │         ^
    7 │ }
