query selectionSetConflictValidStreamQuery {
  me {
    ... on User {
      friends {
        edges @stream(label: "jkdhg", initialCount: 0) {
          node {
            id
          }
        }
      }
    }
  }
}
