==================================== INPUT ====================================
# expected-to-throw
fragment Foo on Query {
  node {
    ...Updatable_comment
  }
}

fragment Updatable_comment on Comment @updatable {
  __id
}
==================================== ERROR ====================================
✖︎ The type of this updatable fragment is `Comment`, but the enclosing selection has type `Node`. A `Node` is not guaranteed to be a `Comment`. Try adding `@alias` to the fragment spread.

  updatable_fragment_spread_concrete_in_matching_abstract.invalid.graphql:4:8
    3 │   node {
    4 │     ...Updatable_comment
      │        ^^^^^^^^^^^^^^^^^
    5 │   }
