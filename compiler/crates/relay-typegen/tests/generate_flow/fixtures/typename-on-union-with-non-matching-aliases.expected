==================================== INPUT ====================================
fragment Foo on Node {
  ... on User {
    __typename
  }
  ... on Comment {
    alias: __typename
  }
  ... on Feedback {
    foo: __typename
  }
}
==================================== OUTPUT ===================================
import type { FragmentType } from "relay-runtime";
declare export opaque type Foo$fragmentType: FragmentType;
export type Foo$data = {
  readonly __typename: "User",
  readonly $fragmentType: Foo$fragmentType,
} | {
  // This will never be '%other', but we need some
  // value in case none of the concrete values match.
  readonly __typename: "%other",
  // This will never be '%other', but we need some
  // value in case none of the concrete values match.
  readonly alias: "%other",
  // This will never be '%other', but we need some
  // value in case none of the concrete values match.
  readonly foo: "%other",
  readonly $fragmentType: Foo$fragmentType,
} | {
  readonly alias: "Comment",
  readonly $fragmentType: Foo$fragmentType,
} | {
  readonly foo: "Feedback",
  readonly $fragmentType: Foo$fragmentType,
};
export type Foo$key = {
  readonly $data?: Foo$data,
  readonly $fragmentSpreads: Foo$fragmentType,
  ...
};
