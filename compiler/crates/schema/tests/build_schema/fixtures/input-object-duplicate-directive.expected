==================================== INPUT ====================================
# expected-to-throw

directive @bar on INPUT_OBJECT

input I @bar @bar {
  id: ID
}

type Query {
  foo(i: I): ID
}
==================================== ERROR ====================================
✖︎ The directive `@bar` can only be used once at this location, but was used 2 times.

  input-object-duplicate-directive.graphql:5:7
    4 │ 
    5 │ input I @bar @bar {
      │       ^
    6 │   id: ID
