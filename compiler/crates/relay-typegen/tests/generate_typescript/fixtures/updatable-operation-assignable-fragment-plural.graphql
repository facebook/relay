query UpdatableQuery @updatable {
  node(id: 4) {
    __typename
    ... on User {
      parents {
        ...Assignable_user
      }
    }
  }
}

fragment Assignable_user on User @assignable {
  __typename
}
