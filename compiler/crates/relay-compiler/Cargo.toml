[project]
name = "relay-compiler"
version = "0.0.0"
edition = "2018"

[dependencies]
graphql-syntax = { path = "../graphql-syntax" }
interner = { path = "../interner" }
serde = { version = "1.0.104", features = ["derive"] }
serde_json = "1.0.45"
