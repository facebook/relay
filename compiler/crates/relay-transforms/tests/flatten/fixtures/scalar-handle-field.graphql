fragment ScalarHandleField on User {
  name @_clientField(handle: "clientName")
  ... on User {
    # should be de-duped
    name @_clientField(handle: "clientName")
    ... on User {
      name @_clientField(handle: "otherClientName")
    }
  }
}
