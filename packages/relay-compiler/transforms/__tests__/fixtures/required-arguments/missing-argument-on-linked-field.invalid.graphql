# expected-to-throw
fragment SomeComment on Comment {
  commentBody {
    ... on PlainCommentBody {
      text {
        text
      }
    }
  }
}

query ViewerQuery {
  node {
    ...SomeComment
  }
}
