==================================== INPUT ====================================
fragment EnabledFoo on User {
  nearest_neighbor  {
    nearest_neighbor @required(action: NONE) {
      name
    }
  }
  assert_nearest_neighbor: nearest_neighbor {
    assert_nearest_neighbor: nearest_neighbor {
      name @required(action: THROW)
    }
  }
}
==================================== OUTPUT ===================================
fragment EnabledFoo on User @assert {
  nearest_neighbor @catch {
    nearest_neighbor @assert {
      name
    }
  }
  assert_nearest_neighbor: nearest_neighbor {
    assert_nearest_neighbor: nearest_neighbor {
      name @assert
    }
  }
}
