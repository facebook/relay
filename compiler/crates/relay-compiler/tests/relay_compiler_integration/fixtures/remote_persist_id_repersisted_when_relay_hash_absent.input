//- component.js
graphql`query RemotePersistNoRelayHashQuery { greeting }`

// No @relayHash in existing artifact: cannot verify the persist ID is still valid,
// so the query is re-persisted and a fresh ID is assigned.

//- __generated__/RemotePersistNoRelayHashQuery.graphql.js
// @relayRequestID pre-existing-persist-id

//- relay.config.json
{
   "language": "flow",
   "schema": "./schema.graphql",
   "src": "./",
   "persistConfig": {
      "url": "relay-test://persist"
   }
}

//- schema.graphql
type Query { greeting: String }
