==================================== INPUT ====================================
# expected-to-throw

directive @bar(x: Int) on OBJECT

type Foo @bar(x: 1) @bar(x: 2) {
  id: ID
}

type Query {
  foo: Foo
}
==================================== ERROR ====================================
✖︎ The directive `@bar` can only be used once at this location, but was used 2 times.

  object-duplicate-directive-differing-args.graphql:5:6
    4 │ 
    5 │ type Foo @bar(x: 1) @bar(x: 2) {
      │      ^^^
    6 │   id: ID
