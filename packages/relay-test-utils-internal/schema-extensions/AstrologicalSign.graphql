# Copyright (c) Meta Platforms, Inc. and affiliates.

# A Client-only type used to model a user's star sign.
type AstrologicalSign {
  id: ID!
  # A field which can be imperatively written
  # used for testing the interaction of client edges and imperatively set
  # fields.
  notes: String
}
