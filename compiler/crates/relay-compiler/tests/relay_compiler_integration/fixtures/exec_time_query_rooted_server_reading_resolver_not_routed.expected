==================================== INPUT ====================================
// Test: a QUERY-ROOTED resolver whose `@rootFragment` reads server data but which
// does NOT declare `@returnFragment` is currently EXCLUDED from
// `has_server_to_client_resolvers`.
//
// This pins a known gap, not desired behaviour. `build_ast.rs` decides the flag with
//
//     Some(parent_type) != self.schema.query_type()
//         || resolver_metadata.return_fragment.is_some()
//
// so a Query parent only qualifies via the magic-fragment carve-out. The exclusion
// exists because a Query-rooted resolver normally reads only client state; the
// carve-out was added for magic fragments, whose transplant puts server selections
// into the operation. A resolver that reads server data through a plain
// `@rootFragment` crosses exactly the same boundary and is still excluded — the
// comment in `build_ast.rs` says so.
//
// Why it matters: without the flag, `LSRelayEnvironmentConfig` never routes the
// operation to `executeWithNetwork`. It lands on the legacy path, where the resolver
// and the network run as two concurrent subscriptions and the root fragment is
// evaluated against an empty store — silently yielding null.
//
// It has not bitten yet only because every Query-rooted shadow resolver we ship IS a
// magic fragment, so the carve-out covers it.
//
// EXPECTED IN THE GOLDEN: `NeedsRoutingQuery` must NOT contain
// `has_server_to_client_resolvers`.
//
// The fix is already available and this fixture is what will catch it landing. The
// compiler ALREADY emits `hasServerField` on a resolver root fragment that reaches a
// server field — see the `resolver_root_fragment_has_server_field` fixture — which is
// exactly the predicate the routing flag should key on instead of `@returnFragment`.
// (That signal is not shown here because emitting a split operation needs
// `resolversSchemaModule`, which this minimal schema cannot register.) When the flag
// switches to it, this fixture must be updated rather than passing silently.

//- NeedsRoutingQuery.js
graphql`query NeedsRoutingQuery @exec_time_resolvers {
  server_reading_greeting
}`

//- Query_server_reading_greeting.js
/**
 * @relayField Query.server_reading_greeting: String
 * @rootFragment QueryServerReadingGreetingFragment
 */
graphql`fragment QueryServerReadingGreetingFragment on Query {
  me {
    name
  }
}`

//- relay.config.json
{
  "language": "typescript",
  "schema": "schema.graphql",
  "featureFlags": {
    "enable_exec_time_resolvers_directive": true
  }
}

//- schema.graphql
type Query {
  me: User
}

type User {
  name: String
}
==================================== OUTPUT ===================================
//-++ __generated__/NeedsRoutingQuery.graphql.ts
/**
 * <auto-generated> SignedSource<<bb7e0184b2814597c421429104a62178>>
 * @lightSyntaxTransform
 */

/* tslint:disable */
/* eslint-disable */
// @ts-nocheck

import { ConcreteRequest } from 'relay-runtime';
import { FragmentRefs } from "relay-runtime";
import { server_reading_greeting as queryServerReadingGreetingResolverType } from "../Query_server_reading_greeting.js";
// Type assertion validating that `queryServerReadingGreetingResolverType` resolver is correctly implemented.
// A type error here indicates that the type signature of the resolver module is incorrect.
(queryServerReadingGreetingResolverType satisfies (
  rootKey: QueryServerReadingGreetingFragment$key,
) => string | null | undefined);
export type NeedsRoutingQuery$variables = Record<PropertyKey, never>;
export type NeedsRoutingQuery$data = {
  readonly server_reading_greeting: string | null | undefined;
};
export type NeedsRoutingQuery = {
  response: NeedsRoutingQuery$data;
  variables: NeedsRoutingQuery$variables;
};

import {server_reading_greeting as queryServerReadingGreetingResolver} from '../Query_server_reading_greeting';
import QueryServerReadingGreetingFragment$normalization from './QueryServerReadingGreetingFragment$normalization.graphql';

const node: ConcreteRequest = {
  "fragment": {
    "argumentDefinitions": [],
    "kind": "Fragment",
    "metadata": null,
    "name": "NeedsRoutingQuery",
    "selections": [
      {
        "alias": null,
        "args": null,
        "fragment": {
          "args": null,
          "kind": "FragmentSpread",
          "name": "QueryServerReadingGreetingFragment"
        },
        "kind": "RelayResolver",
        "name": "server_reading_greeting",
        "resolverModule": queryServerReadingGreetingResolver,
        "path": "server_reading_greeting"
      }
    ],
    "type": "Query",
    "abstractKey": null
  },
  "kind": "Request",
  "operation": {
    "argumentDefinitions": [],
    "kind": "Operation",
    "name": "NeedsRoutingQuery",
    "selections": [
      {
        "name": "server_reading_greeting",
        "args": null,
        "kind": "RelayResolver",
        "storageKey": null,
        "isOutputType": true,
        "resolverInfo": {
          "resolverFunction": queryServerReadingGreetingResolver,
          "rootFragment": QueryServerReadingGreetingFragment$normalization
        }
      }
    ],
    "use_exec_time_resolvers": true
  },
  "params": {
    "cacheID": "7b433e560119af0517a173ff947860c4",
    "id": null,
    "metadata": {},
    "name": "NeedsRoutingQuery",
    "operationKind": "query",
    "text": "query NeedsRoutingQuery {\n  ...QueryServerReadingGreetingFragment\n}\n\nfragment QueryServerReadingGreetingFragment on Query {\n  me {\n    name\n  }\n}\n"
  }
};

(node as any).hash = "600e9cc9cc2d08901656dd7254906d8a";

(node.params.metadata as any).operation = node.operation;

export default node;

//-++ __generated__/QueryServerReadingGreetingFragment.graphql.ts
/**
 * <auto-generated> SignedSource<<8add08b79e5625a706c18da2712e4eb6>>
 * @lightSyntaxTransform
 */

/* tslint:disable */
/* eslint-disable */
// @ts-nocheck

import { ReaderFragment } from 'relay-runtime';
import { FragmentRefs } from "relay-runtime";
export type QueryServerReadingGreetingFragment$data = {
  readonly me: {
    readonly name: string | null | undefined;
  } | null | undefined;
  readonly " $fragmentType": "QueryServerReadingGreetingFragment";
};
export type QueryServerReadingGreetingFragment$key = {
  readonly " $data"?: QueryServerReadingGreetingFragment$data;
  readonly " $fragmentSpreads": FragmentRefs<"QueryServerReadingGreetingFragment">;
};

const node: ReaderFragment = {
  "argumentDefinitions": [],
  "kind": "Fragment",
  "metadata": null,
  "name": "QueryServerReadingGreetingFragment",
  "selections": [
    {
      "alias": null,
      "args": null,
      "concreteType": "User",
      "kind": "LinkedField",
      "name": "me",
      "plural": false,
      "selections": [
        {
          "alias": null,
          "args": null,
          "kind": "ScalarField",
          "name": "name",
          "storageKey": null
        }
      ],
      "storageKey": null
    }
  ],
  "type": "Query",
  "abstractKey": null
};

(node as any).hash = "a2fa2291f958d3d05705e4d4bcef6a95";

export default node;



Artifact Map:
Project: default
  Type: Mapping
  - Source: ExecutableDefinition: NeedsRoutingQuery
    Path: __generated__/NeedsRoutingQuery.graphql.ts
  - Source: ExecutableDefinition: QueryServerReadingGreetingFragment
    Path: __generated__/QueryServerReadingGreetingFragment.graphql.ts
