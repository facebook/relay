==================================== INPUT ====================================
# expected-to-throw

directive @bar on INPUT_FIELD_DEFINITION

input I {
  id: ID @bar @bar
}

type Query {
  foo(i: I): ID
}
==================================== ERROR ====================================
✖︎ The directive `@bar` can only be used once at this location, but was used 2 times.

  input-object-field-duplicate-directive.graphql:6:3
    5 │ input I {
    6 │   id: ID @bar @bar
      │   ^^
    7 │ }
