Input:
var Relay = require('react-relay');
var x = Relay.QL`query { viewer }`;

Output:
'use strict';

var Relay = require('react-relay');
var x = function () {
  return {
    fieldName: 'viewer',
    kind: 'Query',
    metadata: {},
    name: 'QueryWithoutFields',
    type: 'Viewer'
  };
}();
