//- src/component.js

graphql`query componentQuery {
  node(id: "123") {
    ... on User {
      name
    }
  }
}`

//- relay.config.json
{
   "sources": {
      "src": "test_project"
   },
   "projects": {
      "test_project": {
         "schema": "schema.graphql",
         "language": "javascript",
         "excludesExtensions": [
            "[invalid"
         ]
      }
   }
}

//- schema.graphql

type Query {
   node(id: ID!): Node
}

interface Node {
   id: ID!
}

type User implements Node {
   id: ID!
   name: String
}
