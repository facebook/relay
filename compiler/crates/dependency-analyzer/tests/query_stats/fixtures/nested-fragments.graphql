# Tests that a linear chain of nested fragments (F1->F2->F3) produces the
# correct transitive count and max depth.
query DeepQuery {
  me {
    ...F1
  }
}
fragment F1 on User {
  ...F2
  name
}
fragment F2 on User {
  ...F3
  age
}
fragment F3 on User {
  name
}
