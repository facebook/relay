# @generated by autocargo from //relay/oss/crates/intern:intern

[package]
name = "intern"
version = "0.1.0"
authors = ["Facebook"]
edition = "2024"
description = "Intern data into a 32-bit id"
repository = "https://github.com/facebook/relay"
license = "MIT"

[dependencies]
fnv = "1.0"
hashbrown = { version = "0.14.5", features = ["raw", "serde"] }
indexmap = { version = "2.13.0", features = ["arbitrary", "rayon", "serde"] }
once_cell = "1.21"
parking_lot = { version = "0.12.1", features = ["send_guard"] }
schemars = { version = "1.1.0", features = ["indexmap2"] }
serde = { version = "1.0.219", features = ["derive", "rc"] }
serde_bytes = "0.11"
serde_derive = "1.0.185"
smallvec = { version = "1.15", features = ["serde", "union"] }

[dev-dependencies]
bincode = "1.3.3"
rand = { version = "0.9", features = ["small_rng"] }
serde_json = { version = "1.0.140", features = ["alloc", "float_roundtrip", "raw_value", "unbounded_depth"] }

[lints]
rust = { unexpected_cfgs = { check-cfg = ["cfg(memory_consistency_assertions)"], level = "warn" } }
