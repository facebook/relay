==================================== INPUT ====================================
# expected-to-throw
fragment Foo on Query {
  node {
    ...Updatable_actor
  }
}

fragment Updatable_actor on Actor @updatable {
  __id
}
==================================== ERROR ====================================
✖︎ The type of this updatable fragment is `Actor`, but the enclosing selection has type `Node`. A `Node` is not guaranteed to be a `Actor`. Try adding `@alias` to the fragment spread.

  updatable_fragment_spread_abstract_in_different_non_extending_abstract.invalid.graphql:4:8
    3 │   node {
    4 │     ...Updatable_actor
      │        ^^^^^^^^^^^^^^^
    5 │   }
